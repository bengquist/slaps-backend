schema: src/schema
overwrite: true
watch: true
require:
  - ts-node/register
generates:
  ./src/types/types.d.ts:
    plugins:
      - typescript-common
      - typescript-server
      - typescript-resolvers
